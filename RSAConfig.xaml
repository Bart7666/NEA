<Page x:Class="NEA.RSAConfig"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NEA"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RSAConfig">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Viewbox Grid.Column="1">
            <CheckBox x:Name="HexNumberCheckB" Margin="5,0" Content="Hex Input?" Style="{DynamicResource CheckBox}"/>
        </Viewbox>
        <Viewbox Grid.Column="2">
            <CheckBox x:Name="GenerateKeyCheckB"  Content="Generate Key?" Style="{DynamicResource CheckBox}"/>
        </Viewbox>
        <Viewbox Grid.Column="3">
            <Button x:Name="CopyPubliKeyBtn" Margin="4,0" Content="Copy Public Key" Click="CopyPubliKeyBtn_Click" Style="{DynamicResource Button}"/>
        </Viewbox>
        <Viewbox Grid.Column="4">
            <Button x:Name="CopyPrivateKeyBtn" Margin="2.5,0" Content="Copy Private Key" Click="CopyPrivateKeyBtn_Click" Style="{DynamicResource Button}"/>
        </Viewbox>
    </Grid>
</Page>
