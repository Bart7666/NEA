<Window x:Class="NEA.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NEA"
        mc:Ignorable="d"
        Title="SettingsWindow" Height="450" Width="800" WindowStyle="None" Style="{DynamicResource Window}">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30" ResizeBorderThickness="{x:Static SystemParameters.WindowResizeBorderThickness}" />
    </WindowChrome.WindowChrome>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.15*"/>
            <ColumnDefinition Width="0.7*"/>
            <ColumnDefinition Width="0.15*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.15*"/>
            <RowDefinition Height="0.7*"/>
            <RowDefinition Height="0.15*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Column="1" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Viewbox>
                <Label Content="Theme:" Style="{DynamicResource Label}"/>
            </Viewbox>
            <Viewbox Grid.Row="1">
                <Label Content="Font:" Style="{DynamicResource Label}"/>
            </Viewbox>
            <Viewbox Grid.Column="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <RadioButton Grid.Column="0" x:Name="LightModeRButton" Style="{DynamicResource RadioButton}" GroupName="Theme" Content="Light" Margin="0,0,5,0"/>
                    <RadioButton Grid.Column="1" x:Name="DarkModeRButton" Style="{DynamicResource RadioButton}" GroupName="Theme" Content="Dark" Margin="5,0,0,0"/>
                </Grid>
            </Viewbox>
            <Viewbox Grid.Row="2" Grid.Column="1">
                <Button Margin="2" x:Name="ApplyButton" Click="ApplyButton_Click" Content="Apply" Style="{DynamicResource Button}"/>
            </Viewbox>
            
        </Grid>
    </Grid>
</Window>
